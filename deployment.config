# Deployment Configuration for voters-speak

# Deployment settings
DEPLOYMENT_NAME=voters-speak-github-deployment
DEPLOYMENT_BRANCH=main
DEPLOYMENT_ENVIRONMENT=production

# Package settings
PACKAGE_NAME=voters-speak-github-deployment.zip
INCLUDE_FILES=*
EXCLUDE_PATTERNS=.git*,node_modules,*.log,*.tmp

# Artifact settings
ARTIFACT_RETENTION_DAYS=30
